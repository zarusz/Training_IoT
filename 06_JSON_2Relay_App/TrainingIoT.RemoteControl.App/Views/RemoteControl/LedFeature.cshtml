@model TrainingIoT.RemoteControl.App.Models.FeatureModel
<div class="form-group">
    <label class="col-sm-2 control-label">LED</label>
    <div class="col-sm-10">
        @Html.ActionLink("ON", "ChangeLed", new { id = Model.DeviceId, port = Model.Port, on = true }, new { @class = "btn btn-default btn-lg" })
        @Html.ActionLink("OFF", "ChangeLed", new { id = Model.DeviceId, port = Model.Port, on = false }, new { @class = "btn btn-default btn-lg" })
    </div>
</div>

